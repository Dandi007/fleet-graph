# 设计正本回写 GO-26..36：protocol §0.10/§1/§2/§4 + design §7 + context.md 对齐已实现代码

## 背景
`docs/specs/minimal/README.md` 第 12–22 行明说：design.md 与 protocol.md 只落到 **GO-25**，GO-26~36 只在 golden-order 里，凡冲突以 golden-order 为准。而代码已经按 GO-26..36 实现了整整 20 张 DD（enroll/2 六字段、状态归引擎根、PR/worktree 以 DD 为粒度、dispatch 即 DD 启动协议、可跨多 repo、approve 后先看 mergeable）。现在正本与实现对不上，谁读文档谁被误导。`context.md` 第 15 行本来就写了「拍完的动作：一次改 protocol §0.10/§1/§2/§4 + design §1/§7 追加 GO-26~36 决策记录」。

## 要改什么
**纯文档 DD，零代码改动。**

1. `docs/specs/minimal/protocol.md`：
   - §1 改成 `goal.enroll/2` 六字段（schema / work_folder / title / source_branch（goal 级 release 分支名，写一次）/ repos[]（本地路径、remote 必有、target_branch 每 repo 可不同、acceptance）），删掉 `goal_path` / `sessions` / `warn` / 单 `repo` 字段；WF 段按 GO-34 改为「运行时状态归引擎根，WF 只放 goal/design/progress/findings」。
   - §0.10 的 commit 核对表改为 GO-28 的「已 push、HEAD == remote tip、工作树干净」，agent 输入不带 sha；末条「通过即 merge 的归属」按 GO-36 回复定稿为「approve 后先看平台 mergeable，能合就合，冲突才 Merge Agent」。
   - §2 dispatch 改成 GO-36 的 DD 启动协议：Goal Agent Stop 前自建 dd 分支 / worktree / 写 `docs/specs/N-XX.md` 并 push，引擎机械核五条后开 PR、跑基线、起 Impl；可跨多 repo。
   - §4 补 DD 生命周期收尾（GO-35：merged→PR merge/close，failed→close 不合并；删 worktree、删远端 dd 分支）。
2. `docs/specs/minimal/design.md`：§1 节点表把「建分支/开 worktree 由引擎做」改为 GO-36 的归属；§7 追加 GO-26..36 的决策记录条目（编号接在 16 之后）；§1 WF 段按 GO-34 修正。
3. `docs/specs/minimal/context.md`：把「已定」段更新为「已实现」，逐条标出落地它的模块文件名（如 enroll.py / runroot.py / prlifecycle.py / dispatch.py / mergegate.py）；「待用户拍板」的三项（加 repo 走 dispatch 还是只经 goal_steer、多 repo spec 放一处还是每 repo 一份、`models` 留不留）**原样保留并标明仍待人拍**，不要替用户拍。
4. `README.md` 的「优先级：golden-order 26–36 高于 design/protocol」段落随之更新为「已回写，正文与 golden-order 一致」。

## 不要改什么
- **一行代码都不许动**（`src/**`、`tests/**`、`profiles/**`、`pyproject.toml` 全部不碰）。
- 不改 `golden-order.md`（用户原话，只增不改，本 DD 不增）。
- 不替用户拍板上面那三项；不引入任何新的〔推荐〕规则——只把已经拍过的 GO-26..36 落进正文。
- 写进正文的每一条都要和 release 分支上的**实现**一致；不确定的地方去读对应模块的 docstring，不要凭想象写。

## 怎么验收
- `git diff --stat` 只出现 `docs/specs/minimal/` 下的文件。
- protocol.md 全文 grep 不到 `goal.enroll/1`、`goal_path`、`"repo":` 这类已废字段（除非在明确的「已废弃」注记里）。
- README.md 不再声称 design/protocol 只落到 GO-25。
- `make verify` 全绿（纯文档改动应当天然绿）。
